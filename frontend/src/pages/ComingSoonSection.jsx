export default function ComingSoonSection({ title }) {
  return (
    <div className="empty-state">
      <div className="empty-state-title">{title}</div>
      <p>This section is coming in a later stage of the dashboard rollout.</p>
    </div>
  )
}
